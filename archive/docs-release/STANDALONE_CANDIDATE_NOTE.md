# ACI Standalone Project Note

Status: Active

## Purpose

Record that `project/aci/` is the active standalone ACI project root.

## Reading Rule

- treat this directory as the standalone project root
- use repo-relative commands when verifying this repository
- use `common/` only as the copied local rule shelf from `pj-template`

## Current Decision Boundary

- `ACI` is already handled as its own top-level project
- governance concretization for this repository now lives under `docs/governance/`
